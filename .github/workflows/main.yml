name: Workflow ej1

on: push

jobs: 
  prueba:
    runs-on: windows-latest
    steps:
      - name: Descargar pytest
        run: pip install pytest

      - name: Utilizar pytest
        run: pytest test.py

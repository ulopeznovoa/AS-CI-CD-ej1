name: Workflow1

on:
  push:
    branches:
      - main 

jobs:
  tareas:
    runs-on: ubuntu-latest
    steps:
      - name: bajar code
        uses: actions/checkout@v4

      - name: instalar pytest
        run: pip install pytest

      - name: Probar code
        run: pytest test.py
